# TECH_STACK.md

## Dev Port

- Asteria runs locally on `127.0.0.1:3001` during development.
- Tauri `devUrl` is aligned to `http://localhost:3001`.
- This change is temporary and exists to avoid the active Julia Vite server on `3000`.

## Notes

- `3000` is intentionally left untouched so Julia can keep running if needed.
